package de.yard.threed.osm2graph;




/**
 * Created by thomass on 07.06.17.
 */
/*public class MyRoadLine extends MyLineWorldObject {
    
    public MyRoadLine(MyMapGraph segment, TagGroup tags) {
        super(segment);
    }

    @Override
    public void renderTo(GraphicsTarget target) {

           /* if (steps) {
                renderStepsTo(target);
            } else {
                renderLanesTo(target);
            }* /
        Material material = null;//Materials.ASPHALT;

        SimplePolygonXZ p = getOutlinePolygonXZ();
        if (p!=null) {
            target.draw2D("Road", new PolygonOrigin(this.segment), material,p.getVertices(), null);
        }
    }
}*/
